# Centered Header Navigation Design

Use a three-column header grid: logo on the left, desktop navigation in the true center, and mobile controls on the right. The equal outer columns keep the navigation centered within the existing maximum-width header instead of positioning it relative to the logo. Dropdown ownership, hover timing, keyboard behavior, active states, sticky styling, and the mobile drawer remain unchanged.

